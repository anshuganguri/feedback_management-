spring.application.name=feedback-backend

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/feedback_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password= 2013@Mraoanshu

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORS (we will also configure programmatically)
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173

# Logging
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG

server.port=8081